# Configuração do PostgreSQL local via docker-compose.
# O docker-compose já usa esses valores como padrão, então este arquivo é opcional.
# Se quiser customizar, copie para `.env` na raiz e ajuste (server/.env deve refletir).
POSTGRES_USER=spp
POSTGRES_PASSWORD=spp
POSTGRES_DB=spp
